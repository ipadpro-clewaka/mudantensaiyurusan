<!DOCTYPE html>
  <html>
  <head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">
  <title><%= videoData.videoTitle %> (<%= videoData.channelName %>)(わかめtube-mod)</title>
  <meta name="description" content="わかめ">
  <meta property="og:site_name" content="わかめtube | まっしろ">
  <meta property="og:title" content="<%= videoData.videoTitle %>">
  <meta property="og:image" content="https://wakame02m.glitch.me/vi/<%= videoData.videoId %>/maxresdefault.jpg">
  <meta name="theme-color" content="#DC0B00">
  <link rel="shortcut icon" href="/logo.png">
  <meta name="author" content="わかめtube | まっしろ">
  <link rel="apple-touch-icon" sizes="144x144" href="<%= videoData.channelImage %>">
  <link rel="stylesheet" href="/css/bootstrap.min.css">
  <link rel="stylesheet" href="/css/font.css">
  <link rel="stylesheet" href="/css/plyr.css">
  <link rel="stylesheet" href="/css/styles.css">
  <link rel="stylesheet" href="/css/slide.css">
  <link rel="stylesheet" href="https://code.jquery.com/ui/1.12.1/themes/base/jquery-ui.css">
  <link rel="shortcut icon" href="<%= videoData.channelImage %>">
  <script src="https://code.jquery.com/jquery-3.5.1.js"></script>
  <script src="https://code.jquery.com/ui/1.12.1/jquery-ui.js"></script>
  <script src="/js/bootstrap.min.js"></script>
    <link rel="stylesheet" href="/views/background-image.css">
    <link rel="stylesheet" href="/pure-min.css">
    <link rel="stylesheet" href="/grids-responsive-min.css">
    <link rel="stylesheet" href="/ionicons.min.css">
    <link rel="stylesheet" href="/default.css">
    <link rel="stylesheet" href="/header.css">
    <link rel="stylesheet" href="https://code.jquery.com/ui/1.12.1/themes/base/jquery-ui.css" />
    <script src="https://code.jquery.com/jquery-3.5.1.js"></script>
    <script src="https://code.jquery.com/ui/1.12.1/jquery-ui.js"></script>
    <link href="https://vjs.zencdn.net/7.11.4/video-js.css" rel="stylesheet" />
    <script src="https://vjs.zencdn.net/7.11.4/video.min.js"></script>
    <head>
    
    
    <style>
        #myIframe {
  position: fixed;
  right: 10px;  /* 右端から10ピクセルの余白 */
  bottom: 10px; /* 下端から10ピクセルの余白 */
  width: 400px; /* 幅を200ピクセルに設定 */
  height: 300px;/* 高さを150ピクセルに設定 */
  border: none; /* ボーダーなし */
  z-index: 1000; /* 他の要素より前面に表示 */
}
</style>
</head>

</script>
<body class="no-theme">
    <script src="https://accaii.com/sumikaze/script.js" async></script><noscript><img src="//accaii.com/sumikaze/script?guid=on"></noscript>
    <span style="display:none" id="dark_mode_pref"></span>
    <div class="pure-g">
        <div class="pure-u-1 pure-u-md-2-24"></div>
        <div class="pure-u-1 pure-u-md-20-24" id="contents">
            <div class="pure-g navbar h-box">
                <div class="pure-u-1 pure-u-md-12-24 searchbar">
                        
</div>

  <div class="banner">
   <button class="btn btn-link" onclick="window.location.href='/home.pdf'">ホーム🏠</button>
      </form>
    
<br><br><h3 class="yukiyoutube">wakametube-mod</h3>
           
            <div id="player-container" class="h-box">
                <script>
        function toggleContent() {
            var hideElement = document.getElementById("hide");
            var showElement = document.getElementById("show");

            if (hideElement.style.display !== "none") {
                hideElement.style.display = "none";
                showElement.style.display = "block";
            } else {
                hideElement.style.display = "block";
                showElement.style.display = "none";
            }
        }
    </script>
            <div id="hide">
                <video
  id="player"
  class="video-js vjs-default-skin on-video_player player-style-invidious"
  controls
  preload="auto"
  style="outline:none;width:100%;background-color:#000;"
  playsinline
  poster="{% if proxy == 'True' %}/thumbnail?v={{ videoid }}{% else %}https://img.youtube.com/vi/{{ videoid }}/0.jpg{% endif %}"
  data-setup='{}'
>
  {% for videourl in videourls %}
  <source src="<%= videoData.stream_url %>" type="video/mp4">
  {% endfor %}
</video>
            </div>
                <div id="show" style="display: none;">
                <video style="outline:none;width:100%;background-color:#000;" playsinline="" poster="{% if proxy == "True" %}/thumbnail?v=<%= videoData.videoId %>{% else %}https://img.youtube.com/vi/<%= videoData.videoId %>/0.jpg{% endif
                    %}" id="player" class="on-video_player video-js player-style-invidious" controls=""
                    loadedmetadata="settime()" loop="">
                    {% for videourl in videourls%}
                    <source src="<%= videoData.stream_url %>">
                    {% endfor %}
                </video>
                </video></div>
            <div class="h-box">
                <h1><%= videoData.videoTitle %></h1>
            </div>
            <div class="pure-g">
                <div class="pure-u-1 pure-u-lg-1-5">
                    <a href="<%= videoData.stream_url %>" target="_blank">ダウンロード</a><br>
                </div>
                <div class="pure-u-1 pure-u-lg-3-5">
                    <div class="h-box">
                        <a href="/channel/<%= videoData.channelId %>" style="display:block;width:fit-content;width:-moz-fit-content">
                            <div class="channel-profile">

                                <img src="<%= videoData.channelImage %>">

                                <span id="channel-name"><%= videoData.channelName %></span>
                            </div>
                        </a>
                        <div id="description-box">
                            <div id="descriptionWrapper">{{ description | safe}}</div>
                        </div>
                        <hr>
                        <div id="comments">
                        </div>
                    </div>
                </div>
        </div>
    </div>
    <script>$('#searchbox').autocomplete({
            source: function (request, response) {
                {
                    var url = "/suggest?keyword=" + request.term
                    var xhr = new XMLHttpRequest();
                    xhr.open("GET", url);
                    xhr.onload = function () {
                        response(JSON.parse(xhr.responseText));
                    }
                    xhr.send();
                }
            }, delay: 300
        });</script>
        <iframe id="myIframe" src="about:blank"></iframe>

<script>
  // Video.jsの初期化
  var player = videojs('player');

  // 10秒巻き戻しボタンの作成
  var rewindButton = document.createElement('button');
  rewindButton.className = 'vjs-custom-button';
  rewindButton.innerHTML = '⏪ 10秒';

  rewindButton.onclick = function() {
    var currentTime = player.currentTime();
    player.currentTime(Math.max(0, currentTime - 10));
  };

  // 10秒飛ばすボタンの作成
  var forwardButton = document.createElement('button');
  forwardButton.className = 'vjs-custom-button';
  forwardButton.innerHTML = '10秒 ⏩';

  forwardButton.onclick = function() {
    var currentTime = player.currentTime();
    player.currentTime(Math.min(player.duration(), currentTime + 10));
  };

  // コントロールバーにボタンを追加
  player.ready(function() {
    var controlBar = player.controlBar.el();
    controlBar.appendChild(rewindButton);
    controlBar.appendChild(forwardButton);
  });
</script>
</html>
